{
  "name": "smart-logistics-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "prisma:dev": "cross-env NODE_ENV=development prisma",
    "prisma:prod": "cross-env NODE_ENV=production prisma",
    "start:dev": "cross-env NODE_ENV=development ts-node src/server.ts",
    "start:prod": "npm run build && node dist/server.js",
    "prisma:seed": "cross-env NODE_ENV=development prisma db seed",
    "build": "tsc",
    "migrate:prod": "npm run prisma:prod -- migrate deploy",
    "generate:prod": "npm run prisma:prod -- generate",
    "render:build": "npm run migrate:prod && npm run generate:prod && npm run build"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/adapter-pg": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "@types/supertest": "^7.2.0",
    "cross-env": "^10.1.0",
    "prisma": "^7.5.0",
    "supertest": "^7.2.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  }
}
